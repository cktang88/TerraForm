// profile page

import React, {useState} from 'react';
import styled from 'styled-components';

function Profile() {
    return (
        <div>
            <h2>{props.name}</h2>
        </div>

    )
}
export default Profile;